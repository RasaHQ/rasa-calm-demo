parameters:
  num_rephrases: 10
  rephrase_config:
    llm:
      model_name: gpt-4
  model: models
  endpoints: endpoints.yml
  remote-storage: null
  train_frac: null
  output_format: alpaca
  out: output
