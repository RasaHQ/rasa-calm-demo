flows:
  replace_card:
    description: the user needs to replace their card
    name: replace_card
    nlu_trigger:
      - intent:
          name: replace_card
          confidence_threshold: 0.9
    steps:
      - collect: confirm_correct_card
        ask_before_filling: true
        next:
          - if: slots.confirm_correct_card
            then:
              - action: action_replace_card_eligibility
                next:
                - if: slots.replace_card_eligibility = 'not_eligible_child'
                  then:
                    - action: utter_ineligible_child_card
                      next: END
                - if: slots.replace_card_eligibility = 'no_card_on_file'
                  then:
                    - link: no_eligible_card
                - else:
                    - call: replace_eligible_card
                      next: END
          - else:
              - action: utter_relevant_card_not_linked
                next: END
