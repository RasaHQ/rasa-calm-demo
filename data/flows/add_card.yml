flows:
  add_card:
    description: this flow is triggered when users explicitly wants to add a new card to his account
    name: add a card
    nlu_trigger:
      - intent:
          name: add_card
          confidence_threshold: 0.9
    steps:
      - action: utter_card_added
